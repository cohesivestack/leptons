import { CoverageInfo } from "../coverage-info";

export const counterIncrement: CoverageInfo[] = [
  { style: "counter-increment",
    values: "none|{id}|initial|inherit",
    skip: true,
    note: "No plans to cover this in the future."
  }
]