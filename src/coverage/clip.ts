import { CoverageInfo } from "../coverage-info";

export const clip: CoverageInfo[] = [
  { style: "clip",
    values: "auto|{shape}|initial|inherit",
    skip: true,
    note: "No plans to cover for now."
  }
]