import { CoverInfo } from "../cover-info";

export const imageRendering: CoverInfo[] = [
  { style: "image-rendering",
    values: "auto|smooth|high-quality|crisp-edges|pixelated|initial|inherit"
  }
]