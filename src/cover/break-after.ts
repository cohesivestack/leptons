import { CoverInfo } from "../cover-info";

export const breakAfter: CoverInfo[] = [
  { style: "break-after",
    values: "auto|all|always|avoid|avoid-column|avoid-page|avoid-region|column|left|page|recto|region|right|verso|initial|inherit"
  }
]